# 아이스브레이킹 가이드 — 바이브코딩 입문 교육용

> 비개발자 대상 AI/바이브코딩 교육의 도입부 활동
> IT 총판업 실무자 40명 기준 / 교육 당일 첫 15~20분

---

## 왜 이 아이스브레이킹인가

일반적인 바이브코딩 교육의 아이스브레이킹:
- AI 이미지 퀴즈, 프롬프트 배틀, 자기소개...
- 재미있지만 "이게 내 업무와 무슨 상관이지?" 단절 발생

이 아이스브레이킹의 원칙:
- **첫 10분에 "AI가 실제 업무에 쓸 수 있다"는 걸 직접 보여준다**
- 재미보다 실용, 게임보다 현실감

---

## 다운로드 폴더 정리 데모 (15~20분)

### 핵심 아이디어

대부분의 직장인 PC에는 다운로드 폴더가 있고, 대부분 엉망이다.
PDF, PPTX, XLSX, ZIP, 이미지들이 뒤섞인 그 폴더를
**AI가 작성한 Python 코드 한 번으로 정리하는 것**을 보여준다.

이게 주는 메시지:
> "코딩을 몰라도, AI에게 설명하면 AI가 코드를 만들고, 그 코드가 실제 파일을 정리해줍니다."

---

### 진행 방법 (총 15분)

#### PART A — 강사 데모 (8분)

**1. 문제 제시 (2분)**

슬라이드 또는 실제 화면:

```
[강사 화면: 다운로드 폴더]
파일 197개
report_final.pdf
report_final2.pdf
견적서_20240318.xlsx
presentation.pptx
KakaoTalk_Photo_2024_...jpg
(이하 생략)
```

강사 멘트:
> "이 폴더, 여러분 컴퓨터에도 이런 거 하나씩 있으시죠?
> 이걸 손으로 정리하면 얼마나 걸릴 것 같아요?
> (참가자 답 받기) 맞아요, 한 시간은 기본이죠.
> 오늘 우리가 이걸 30초로 줄이는 방법을 배울 거예요."

**2. AI에게 요청 (3분)**

Claude.ai 또는 Antigravity 화면 공유:

```
[강사가 AI에 입력하는 것]

내 다운로드 폴더에 파일이 뒤죽박죽 섞여 있어.
Python 코드를 만들어줘.
이 코드가 하는 일:
- PDF 파일 → "PDF" 폴더로 이동
- PPTX 파일 → "PPTX" 폴더로 이동
- XLSX 파일 → "엑셀" 폴더로 이동
- JPG, PNG → "이미지" 폴더로 이동
- 나머지 → "기타" 폴더로 이동

폴더가 없으면 자동으로 만들어줘.
실행하면 "정리 완료: OO개 파일"이라고 출력해줘.
```

**3. 코드 실행 (3분)**

강사가 AI가 만든 코드를 실행:

```python
# AI가 생성한 코드 (강사 화면에 표시)
import os, shutil
from pathlib import Path

downloads = Path.home() / "Downloads"
folders = {
    "PDF": [".pdf"],
    "PPTX": [".pptx", ".ppt"],
    "엑셀": [".xlsx", ".xls"],
    "이미지": [".jpg", ".png", ".jpeg"],
}

count = 0
for file in downloads.iterdir():
    if file.is_file():
        for folder_name, extensions in folders.items():
            if file.suffix.lower() in extensions:
                dest = downloads / folder_name
                dest.mkdir(exist_ok=True)
                shutil.move(str(file), str(dest / file.name))
                count += 1
                break

print(f"정리 완료: {count}개 파일")
```

실행 전후 화면 비교:
- 전: 파일 197개 뒤섞임
- 후: PDF/ PPTX/ 엑셀/ 이미지/ 기타/ 폴더로 정리

강사 멘트:
> "제가 코딩한 것 아닙니다. AI에게 말로 설명했고, AI가 코드를 만들었고, 그걸 실행했어요.
> 이게 바이브코딩입니다."

---

#### PART B — 참가자 연결 (7분)

**4. 오늘의 틀 설명 (3분)**

강사 멘트:
> "오늘 우리가 할 것이 정확히 이겁니다.
> 방금은 '다운로드 폴더 정리'였지만,
> 오늘 여러분은 본인의 실제 업무를 정리합니다.
> 
> 폴더와 파일을 만들고,
> 내가 하는 일을 단계별로 씁니다.
> 그걸 AI에 넣으면 — 그게 여러분의 첫 번째 업무 도구입니다."

**5. 클론 & 환경 확인 (4분)**

(사전 설정이 완료된 경우 진행)

```
강사:
"지금 노트북 열어주세요.
GitHub Desktop 켜시고,
오늘 실습 폴더가 보이는 분 손들어주세요."
```

- 열린 사람: 다음 진행
- 안 열린 사람: 보조 강사 또는 조장이 즉시 지원

---

## 아이스브레이킹 데모 준비 사항 (강사용)

### 사전 준비 파일

교육 당일 데모용으로 준비:

```
/demo_downloads/      ← 예시 폴더 (파일 20~30개 혼합)
  report_2024.pdf
  견적서_A사.xlsx
  proposal.pptx
  KakaoTalk_Photo.jpg
  ... (의도적으로 뒤섞어 둠)
```

### AI 프롬프트 (데모용 — 미리 테스트 완료)

```
내 다운로드 폴더에 파일이 뒤죽박죽 섞여 있어.
Python 코드를 만들어줘.
이 코드가 하는 일:
- PDF 파일 → "PDF" 폴더로 이동
- PPTX 파일 → "PPTX" 폴더로 이동
- XLSX 파일 → "엑셀" 폴더로 이동
- JPG, PNG → "이미지" 폴더로 이동
- 나머지 → "기타" 폴더로 이동

폴더가 없으면 자동으로 만들어줘.
실행하면 "정리 완료: OO개 파일"이라고 출력해줘.
```

### 대안 (Python 없는 환경)

Python 미설치 참가자 다수인 경우 → 파일 실행 데모 생략, 대신:
- AI가 코드를 생성하는 과정만 화면으로 보여줌
- "이걸 실행하면 정리가 됩니다" 설명으로 마무리
- 실제 실행은 교육 후 혼자 해보도록 안내

---

## 아이스브레이킹 후 1번 작업 연결

> "방금 다운로드 폴더 정리처럼,
> 오늘 여러분은 본인의 업무를 폴더로 정리하고 워크플로우를 만들 겁니다.
> 그 결과물을 AI에 넣으면, AI가 자동화 도구를 만들어줍니다.
> 지금부터 그 첫 번째 작업을 시작합니다."

→ **1번 작업: 내 업무를 구조화하라** 로 넘어감

---

## 시간 조정 (Option A / B)

| 옵션 | 아이스브레이킹 | 구성 |
|------|-------------|------|
| Option A (4시간) | 15분 | 강사 데모 8분 + 환경 확인 7분 |
| Option B (6시간) | 20분 | 강사 데모 10분 + 환경 확인 + 짧은 Q&A 10분 |

**압축할 경우**: PART B 생략, PART A 데모만 10분으로 진행
**시간 여유 있을 경우**: 참가자 한 명이 직접 프롬프트 써보기 추가 (3분)

---

## 강사가 피해야 할 것

1. **Python 설명에 5분 이상 쓰기** — 이 활동의 목적은 Python 설명이 아님
2. **코드를 읽어주기** — "이 줄은 폴더를 만들고..." → 불필요, 오히려 겁줌
3. **"이해하셨나요?" 확인** — 이해가 목표가 아니라 '봤다'가 목표
4. **설치 오류에 멈추기** — 설치 안 된 사람은 강사 화면 같이 보며 진행

**핵심 메시지는 하나**:
> "코딩 몰라도 됩니다. AI에게 설명할 수 있으면 됩니다."
