# CH2. 진짜 쉬운 거 만들어보기

> 소요 시간: 25~30분
> 목표: "나도 할 수 있다"는 첫 성공 경험 만들기
> 과제 완료 기준: AI로 생성한 이메일 초안 + HTML 파일이 브라우저에서 열린 상태

---

## 이 챕터에서 할 것

```
과제 1. AI 도구 첫 접속
과제 2. 이메일 초안 생성 (30초 성공)
과제 3. 프롬프트 수정 실습 (결과 개선하기)
과제 4. HTML 파일 만들기
과제 5. 브라우저에서 열기 + 레포에 저장
```

---

## 과제 1. AI 도구 첫 접속 (3분)

### Antigravity 접속 (권장)

1. Chrome 또는 Edge 브라우저 열기
2. 주소창에 입력: `https://antigravity.ai`
3. 이메일로 로그인 (사전에 가입 완료됐어야 함)
4. 새 프로젝트 화면이 보이면 준비 완료

### Claude.ai 접속 (대안)

Antigravity 접속이 안 될 경우:
1. 주소창에 입력: `https://claude.ai`
2. 로그인
3. 새 대화 화면의 입력창이 보이면 준비 완료

> 둘 다 안 되는 경우: 스마트폰 핫스팟으로 전환 후 재시도.
> 회사 네트워크 방화벽이 막는 경우가 있습니다.

---

## 과제 2. 이메일 초안 생성 (10분)

### 상황 설정

> 상황: 우리 회사가 파트너사 **한국IT솔루션**에 납품한 Cisco 스위치 2대에서
> 결함이 발견됐습니다. 내일 오전 교체품을 발송할 예정입니다.
> 담당자는 **김철수 팀장**입니다.

### 프롬프트 입력

AI 입력창에 아래 내용을 **그대로 복사해서 붙여넣기** 합니다:

```
파트너사에 보내는 이메일을 써줘.

상황: 납품한 Cisco 스위치 C9200 2대에서 결함이 발견됨
수신: 한국IT솔루션 김철수 팀장
처리 방법: 내일 오전 교체품 발송 예정
톤: 정중하고 신뢰감 있게
길이: 본문 3~4문장
```

`Enter` 또는 전송 버튼 클릭.

### 결과 확인

AI가 만들어준 이메일을 읽어봅니다.

예시 결과:
```
제목: [긴급] Cisco 스위치 납품 불량 건 및 교체 안내

김철수 팀장님,

이번 납품 건에서 Cisco 스위치 C9200 2대의 결함이 확인되어
먼저 진심으로 사과의 말씀을 드립니다.

내일(5월 6일) 오전 중으로 정상 제품을 교체 발송해 드릴 예정이며,
불량품 회수는 교체품 전달 시 함께 진행하겠습니다.

불편을 드려 다시 한번 죄송합니다.
언제든지 연락 주시면 신속하게 대응하겠습니다.

아이넷뱅크 영업팀 드림
```

**여기까지 완료하면 첫 번째 성공입니다.**

조원들과 결과를 비교해보세요. 같은 프롬프트를 넣어도 결과가 조금씩 다릅니다.

---

## 과제 3. 프롬프트 수정 실습 (7분)

AI의 결과를 내가 원하는 방향으로 **추가 요청**하는 연습입니다.

### 수정 요청 방법

AI와의 대화를 이어가면 됩니다. 새로 시작하지 않아도 됩니다.

**연습 1 — 더 짧게:**
```
더 짧게 해줘. 본문 2문장으로.
```

**연습 2 — 톤 변경:**
```
좀 더 격식 있게 바꿔줘. 영업부장이 쓰는 느낌으로.
```

**연습 3 — 내용 추가:**
```
교체 완료 후 품질 검사 절차도 한 줄 추가해줘.
```

**연습 4 — 내 상황으로 변경:**
```
수신자를 "박소현 과장"으로, 제품을 "Zebra TC52 단말기 5대"로 바꿔줘.
```

### 포인트

> 틀려도 됩니다. 마음에 안 들면 다시 요청하면 됩니다.
> AI는 지치지 않습니다.

---

## 과제 4. HTML 파일 만들기 (7분)

### 무엇을 만드나

"미팅 준비 카드" — 고객 미팅 전에 빠르게 채우는 1페이지 메모 폼.

브라우저에서 열리는 HTML 파일이고, 팀원들에게 파일로 공유 가능합니다.

### 프롬프트 입력

AI 입력창에 아래 내용을 복사해서 붙여넣기:

```
"미팅 준비 카드"라는 제목의 HTML 파일을 만들어줘.

포함할 항목:
- 미팅 일시 (날짜 입력칸)
- 고객사명 (텍스트 입력칸)
- 담당자 이름 (텍스트 입력칸)
- 핵심 질문 3개 (텍스트 입력칸 3개)
- 특이사항 메모 (여러 줄 입력 가능한 텍스트 영역)
- "내용 복사" 버튼 (누르면 입력된 내용이 클립보드에 복사됨)

디자인 조건:
- 깔끔하고 실용적인 비즈니스 스타일
- 배경 흰색, 포인트 색상은 파란색
- 모바일에서도 잘 보이게
```

### 결과 코드 복사

AI가 HTML 코드를 만들어주면:

1. 코드 블록 위에 있는 **복사 버튼** 클릭 (또는 전체 선택 후 Ctrl+C)
2. 복사 완료

---

## 과제 5. 파일로 저장 + 브라우저에서 열기 + 레포에 저장 (5분)

### 파일로 저장

**Windows:**
1. 바탕화면에서 우클릭 → `새로 만들기` → `텍스트 문서`
2. 파일명을 `미팅준비카드.html`로 변경 (확장자 `.html` 중요)
3. 파일을 메모장으로 열기
4. 복사한 코드 붙여넣기 (Ctrl+V)
5. 저장 (Ctrl+S)

**Mac:**
1. TextEdit 실행
2. `Format` 메뉴 → `Make Plain Text` 클릭 (중요!)
3. 복사한 코드 붙여넣기
4. `파일` → `다른 이름으로 저장`
5. 파일명: `미팅준비카드.html`, 형식: 텍스트 파일
6. 저장

> 파일명 끝이 반드시 `.html`이어야 합니다.
> `.txt`로 저장하면 브라우저에서 열리지 않습니다.

### 브라우저에서 열기

저장한 `미팅준비카드.html` 파일을:
- Chrome 또는 Edge 브라우저 창으로 **드래그 앤 드롭**
- 또는 파일 우클릭 → `Chrome으로 열기`

이런 화면이 나와야 합니다:

```
┌──────────────────────────────────────┐
│          미팅 준비 카드               │
│                                      │
│ 미팅 일시: [         ]               │
│ 고객사: [           ]               │
│ 담당자: [           ]               │
│                                      │
│ 핵심 질문 1: [      ]               │
│ 핵심 질문 2: [      ]               │
│ 핵심 질문 3: [      ]               │
│                                      │
│ 특이사항: [                  ]      │
│                                      │
│        [ 내용 복사 ]                 │
└──────────────────────────────────────┘
```

**직접 내용을 입력하고 "내용 복사" 버튼도 눌러보세요.**

### 레포에 저장

1. 만든 `미팅준비카드.html` 파일을
   `vibe-coding-workshop/01_영업미팅준비/` 폴더로 이동 또는 복사
2. GitHub Desktop으로 돌아오면 변경된 파일이 보임
3. Summary에 입력: `미팅 준비 카드 HTML 추가`
4. `Commit to main` 클릭

---

## 챕터 완료 체크리스트

```
□ Claude.ai 또는 Antigravity에 접속했다
□ 이메일 초안 프롬프트를 입력하고 결과를 받았다
□ 수정 요청을 최소 2번 해봤다
□ HTML 파일 생성 프롬프트를 입력하고 코드를 받았다
□ 파일을 .html로 저장했다
□ 브라우저에서 열어서 입력칸이 동작하는 걸 확인했다
□ 01_영업미팅준비/ 폴더에 파일을 넣고 커밋했다
```

---

## 잘 안 되는 경우 대처법

| 상황 | 해결 방법 |
|------|---------|
| AI 접속이 안 됨 | 핫스팟으로 전환, 또는 조원 화면 같이 보기 |
| 코드가 너무 길어서 못 찾겠음 | AI에게 "코드만 다시 한번 줘" 라고 입력 |
| 파일이 .txt로 저장됨 | 파일명 변경: 오른쪽 클릭 → 이름 바꾸기 → .html로 수정 |
| 브라우저에서 코드가 그대로 보임 | 파일 확장자가 .html인지 확인, 다시 브라우저로 드래그 |
| "내용 복사" 버튼이 안 됨 | AI에게 "복사 버튼이 동작하지 않아, 고쳐줘" 라고 입력 |

---

## 심화 (시간이 남는 분)

아래 프롬프트로 더 발전시켜 보세요:

```
이 미팅 준비 카드에 기능을 추가해줘.
추가할 것:
1. "저장" 버튼 — 입력 내용을 브라우저가 기억하게 (localStorage)
2. "초기화" 버튼 — 모든 입력칸 비우기
3. 상단에 오늘 날짜 자동 표시
```
