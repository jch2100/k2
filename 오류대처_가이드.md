# 오류 대처 가이드 — 초보자용

> "오류는 실패가 아닙니다. AI에게 물어보면 되는 신호입니다."
> 이 가이드는 교육 중 발생하는 모든 오류 상황에 대한 대처법입니다.

---

## 가장 먼저 기억할 것: 황금 원칙 3가지

```
원칙 1. 오류 메시지를 그대로 복사해서 AI에게 붙여넣는다
원칙 2. 혼자 5분 이상 막히면 반드시 손을 든다
원칙 3. 오류는 나쁜 것이 아니다 — 실무에서도 매일 생긴다
```

### 오류 발생 시 AI에게 보내는 기본 템플릿

```
아래 오류가 발생했어. 원인이 뭔지, 어떻게 고치면 되는지 알려줘.

[오류 메시지 전체 복사·붙여넣기]

내 상황:
- 운영체제: Windows 11 / Mac
- 하려던 것: [예: pip install 실행 중]
```

---

## PART 1. 터미널·명령어 오류

### 오류 1: `python` 또는 `python3`을 인식하지 못함

**증상:**
```
'python'은(는) 내부 또는 외부 명령, 실행할 수 있는 프로그램, 또는
배치 파일이 아닙니다.
```
또는
```
zsh: command not found: python
```

**원인:** Python이 설치됐지만 시스템 PATH에 등록되지 않았음

**해결 방법 (Windows):**
1. Python 재설치:
   - 기존 Python 제어판에서 제거
   - 설치 파일 다시 실행
   - 설치 화면 첫 페이지에서 **"Add Python to PATH"** 체크박스 반드시 체크
   - Install Now 클릭
2. 터미널 완전히 닫고 새로 열기
3. `python --version` 재시도

**해결 방법 (Mac):**
```
python3 --version
```
Mac에서는 `python` 대신 `python3`를 쓰면 됩니다.

**USB 오프라인 해결:** USB의 `설치파일/python-3.12.0-amd64.exe` 실행 (아래 USB 가이드 참조)

---

### 오류 2: pip install 중 SSL 오류

**증상:**
```
SSL: CERTIFICATE_VERIFY_FAILED
Could not fetch URL https://pypi.org/...
```

**원인:** 회사 방화벽 또는 보안 프록시가 패키지 다운로드를 차단

**해결 방법 1 — 신뢰 호스트 옵션 추가:**
```
pip install --trusted-host pypi.org --trusted-host files.pythonhosted.org [패키지명]
```

**해결 방법 2 — USB 오프라인 패키지 사용:**
```
pip install --no-index --find-links=D:\usb_packages\ [패키지명]
```
(D: 는 USB 드라이브 경로로 변경)

**해결 방법 3 — 모바일 핫스팟으로 전환:**
회사 네트워크를 끊고 스마트폰 핫스팟에 연결 후 재시도

---

### 오류 3: `ModuleNotFoundError`

**증상:**
```
ModuleNotFoundError: No module named 'pandas'
ModuleNotFoundError: No module named 'openpyxl'
```

**원인:** 필요한 Python 패키지가 설치되지 않음

**해결 방법:**
```
pip install pandas
pip install openpyxl
pip install pdf2zh
```

여러 개를 한 번에:
```
pip install pandas openpyxl requests
```

**AI에게 물어볼 때:**
```
이 오류가 발생했어: ModuleNotFoundError: No module named 'pandas'
어떤 명령어로 설치하면 돼?
```

---

### 오류 4: `SyntaxError`

**증상:**
```
SyntaxError: invalid syntax
SyntaxError: unexpected EOF while parsing
```

**원인:** AI가 생성한 코드에 오타 또는 복사 중 일부가 잘림

**해결 방법:**
1. AI에게 오류 메시지 + 해당 코드를 함께 보냄:
```
이 코드를 실행하면 SyntaxError가 발생해.

[오류 메시지]

[코드 전체]

어디가 문제인지 찾아서 고쳐줘.
```
2. 코드 재생성 요청:
```
처음부터 다시 만들어줘. 위에서 오류가 났어.
```

---

### 오류 5: 권한(Permission) 오류

**증상:**
```
PermissionError: [WinError 5] 액세스가 거부되었습니다
Permission denied: '/usr/local/lib/...'
```

**원인:** 관리자 권한 없이 시스템 폴더에 설치 시도

**해결 방법 (Windows):**
```
pip install --user [패키지명]
```
(`--user` 옵션은 관리자 권한 없이 현재 사용자 폴더에 설치)

또는 터미널을 "관리자 권한으로 실행" 후 재시도

**해결 방법 (Mac):**
```
sudo pip3 install [패키지명]
```
비밀번호 입력 후 설치

---

## PART 2. Git / GitHub 오류

### 오류 6: 클론(Clone) 실패

**증상 A:**
```
fatal: repository 'https://github.com/...' not found
```

**원인:** URL 오타 또는 레포가 비공개(Private)

**해결:**
- URL을 다시 확인 (한 글자라도 다르면 실패)
- 강사에게 정확한 URL 다시 받기

---

**증상 B:**
```
Failed to connect to github.com port 443
unable to access 'https://github.com/...': Could not resolve host
```

**원인:** 네트워크 연결 문제 또는 방화벽 차단

**해결:**
1. 인터넷 연결 확인 (브라우저에서 google.com 열어보기)
2. 모바일 핫스팟으로 전환
3. GitHub Desktop 재시작

---

### 오류 7: 커밋(Commit) 실패

**증상:**
```
Author identity unknown
Please tell me who you are.
```

**원인:** Git에 이름/이메일이 설정되지 않음

**해결 (터미널에서):**
```
git config --global user.name "홍길동"
git config --global user.email "honggildong@company.com"
```

또는 GitHub Desktop에서 자동으로 처리됨 (로그인 상태면 OK)

---

### 오류 8: 푸시(Push) 실패 — 인증 오류

**증상:**
```
remote: Support for password authentication was removed
Authentication failed
```

**원인:** GitHub 계정 토큰 만료 또는 로그인 안 됨

**해결 (GitHub Desktop):**
1. `File` → `Options` → `Accounts` 탭
2. 기존 계정 `Sign out`
3. 다시 `Sign in to GitHub.com`
4. 브라우저에서 인증 완료

---

## PART 3. 브라우저 / AI 도구 오류

### 오류 9: Claude.ai / Antigravity 접속 불가

**증상:**
- 페이지가 열리지 않음
- "이 사이트에 연결할 수 없음"

**원인:** 회사 방화벽 차단

**해결:**
1. **즉시 해결**: 스마트폰 핫스팟 켜기
   - 아이폰: 설정 → 개인용 핫스팟 → 켬
   - 갤럭시: 설정 → 연결 → 모바일 핫스팟 → 켬
2. 노트북 Wi-Fi를 핫스팟에 연결
3. Claude.ai 재접속

**대안 도구 순서:**
```
1순위: Antigravity (antigravity.ai)
2순위: Claude.ai (claude.ai)
3순위: ChatGPT (chatgpt.com)
4순위: 강사 화면 같이 보며 진행
```

---

### 오류 10: AI 응답 한도 초과

**증상:**
```
You've reached your message limit for Claude.ai
Free plan limit reached
```

**원인:** 무료 계정의 일일 사용 한도 초과

**해결:**
1. 다른 이메일로 새 무료 계정 생성
2. 옆 조원 계정 화면 같이 보기
3. ChatGPT (chatgpt.com) 무료 계정으로 전환

---

### 오류 11: HTML 파일이 코드로 보임

**증상:** 브라우저에서 HTML 파일을 열었는데 예쁜 화면이 아니라 코드가 그대로 보임

**원인:** 파일이 `.txt`로 저장됨

**해결:**
1. 파일 탐색기에서 파일 우클릭 → `이름 바꾸기`
2. `미팅준비카드.txt` → `미팅준비카드.html` 로 변경
3. 확장자 변경 경고창 → `예` 클릭
4. 브라우저로 다시 드래그

> **주의 (Windows):** 파일 확장자가 숨겨져 있는 경우
> 파일 탐색기 → `보기` → `파일 이름 확장명` 체크

---

### 오류 12: HTML 버튼이 동작하지 않음

**증상:** "결과 생성" 버튼을 눌렀는데 아무것도 안 일어남

**원인:** AI가 생성한 JavaScript 코드에 오류

**해결:**
AI에게 다음과 같이 요청:
```
이 HTML 파일에서 [버튼 이름] 버튼을 클릭해도 아무 반응이 없어.
JavaScript 오류인 것 같아.
버튼이 동작하도록 고쳐줘.

[HTML 코드 전체 붙여넣기]
```

---

## PART 4. VSCode 오류

### 오류 13: VSCode 한국어가 깨짐

**증상:** 파일을 열었을 때 한글이 `???` 또는 이상한 문자로 보임

**원인:** 파일 인코딩 불일치 (UTF-8 vs EUC-KR)

**해결:**
1. VSCode 우하단에 `UTF-8` 또는 `EUC-KR` 클릭
2. `Reopen with Encoding` → `UTF-8` 선택
3. 그래도 안 되면 `Save with Encoding` → `UTF-8` 선택

---

### 오류 14: VSCode Extension 설치 안 됨

**증상:**
```
Error while installing extension: ...
Extension installation failed
```

**원인:** 인터넷 차단 또는 프록시 설정

**해결:**
1. 강사에게 `.vsix` 확장 파일 USB로 받기
2. VSCode에서: `Extensions` 탭 → 우상단 `···` → `Install from VSIX`
3. USB의 `.vsix` 파일 선택

---

## PART 5. PDF 번역 오류

### 오류 15: pdf2zh 번역 중 한국어가 안 나옴

**증상:** 번역 결과가 영어 그대로이거나 이상한 문자

**해결:**
```
pdf2zh sample.pdf -t ko
```
`-t ko` 옵션이 빠진 경우. 타겟 언어 명시 필요.

또는 웹 버전에서 Target Language를 `Korean`으로 설정했는지 확인.

---

### 오류 16: PDF 파일 업로드 실패

**증상:** 파일 업로드 버튼이 안 되거나 "파일 크기 초과" 오류

**해결:**
1. 파일 크기 확인 — 5MB 이상이면 압축 필요
2. Adobe Reader 또는 온라인 PDF 압축 도구로 압축 후 재시도
3. PDF를 여러 페이지로 분할해서 나눠서 번역

---

## PART 6. 오류별 빠른 대응표

| 오류 상황 | 즉시 해결 | 안 되면 |
|---------|---------|--------|
| python 인식 안 됨 | USB 설치 파일 실행 | 강사 호출 |
| pip install 실패 | 핫스팟 전환 후 재시도 | USB 오프라인 패키지 |
| 클론 안 됨 | 핫스팟 전환 | URL 재확인 |
| AI 접속 안 됨 | 핫스팟 전환 | 다른 AI 도구 사용 |
| AI 한도 초과 | 새 계정 or 조원 계정 | ChatGPT로 전환 |
| HTML 코드로 보임 | 확장자 .html로 변경 | 강사 호출 |
| 버튼 안 됨 | AI에게 오류 설명 | 코드 재생성 요청 |
| 한글 깨짐 | VSCode 인코딩 UTF-8 | 메모장으로 다시 저장 |
| 권한 오류 | --user 옵션 추가 | 관리자 권한 실행 |

---

## PART 7. AI에게 오류를 물어보는 법

### 좋은 질문 vs 나쁜 질문

**❌ 나쁜 질문:**
```
오류가 났어요.
```

**✅ 좋은 질문:**
```
Python 코드를 실행하다가 아래 오류가 났어.

오류 메시지:
FileNotFoundError: [Errno 2] No such file or directory: 'sales_data.csv'

내 상황:
- Windows 11
- 터미널에서 python sort_files.py 를 실행함
- 파일은 D:\Documents\workshop\ 폴더에 있음

어떻게 고치면 돼?
```

### AI가 고쳐준 코드가 또 오류날 때

```
고쳐준 코드로 다시 했는데 또 오류가 났어.

새 오류 메시지:
[새 오류 메시지]

고친 코드:
[현재 사용 중인 코드]

계속 안 돼. 다른 방법으로 해줘.
```

### "AI도 모를 때" 판단 기준

AI가 3번 이상 같은 오류를 고치지 못하면 → **강사에게 손들기**

---

## 강사·보조강사용 — 현장 오류 대응 스크립트

### 오류 발견 시 공지 멘트

```
"잠깐, 지금 [오류 상황]이 발생한 분들 계시죠?
이거 사실 실무에서도 자주 생기는 상황이에요.
지금 어떻게 해결하는지 같이 보겠습니다."
```

오류를 숨기거나 당황하지 말고, **"이 상황이 교육 내용의 일부"**로 자연스럽게 전환합니다.

### 5분 규칙

- 수강생이 혼자 막힌 지 5분이 넘으면 → 강사/보조강사 즉시 개입
- 같은 오류가 3명 이상 → 전체 공지 후 함께 해결
- 해결 불가능한 오류 → 해당 수강생은 옆 조원과 화면 공유로 진행
