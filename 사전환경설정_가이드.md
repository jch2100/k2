# 사전 환경 설정 가이드

> 아이넷뱅크 바이브코딩 교육 — 수강생 사전 준비
> 교육 3일 전 발송 / 소요 시간: 약 20~30분

---

## 오늘 설치할 것 (최소 환경)

| 도구 | 필수 여부 | 이유 |
|------|---------|------|
| GitHub 계정 | ★ 필수 | 실습 파일 받기 + 결과물 저장 |
| GitHub Desktop | ★ 필수 | 클릭 한 번으로 실습 폴더 열기 |
| Python | 권장 | 아이스브레이킹 데모 직접 실행하려면 필요 |
| Antigravity 또는 Claude.ai 계정 | ★ 필수 | AI 도구 (교육 핵심 도구) |

---

## STEP 1 — GitHub 계정 만들기 (5분)

1. [https://github.com](https://github.com) 접속
2. `Sign up` 클릭
3. 이메일 / 비밀번호 / 사용자 이름 입력
4. 이메일 인증 완료

> **참고**: 회사 이메일로 만들어도 됩니다. 무료 계정으로 충분합니다.

---

## STEP 2 — GitHub Desktop 설치 (5분)

1. [https://desktop.github.com](https://desktop.github.com) 접속
2. 운영체제에 맞는 버전 다운로드 (Windows / Mac)
3. 설치 후 실행
4. GitHub 계정으로 로그인

> **왜 GitHub Desktop?**: 명령어 없이 버튼 클릭만으로 파일을 받고 저장할 수 있습니다.

---

## STEP 3 — 실습 레포 클론하기 (5분)

강사가 공유하는 레포 주소로 진행:

```
레포 주소: https://github.com/[강사계정]/vibe-coding-workshop
```

**GitHub Desktop에서 클론하는 방법**:
1. GitHub Desktop 실행
2. 상단 메뉴 → `File` → `Clone repository`
3. `URL` 탭 클릭
4. 위 레포 주소 붙여넣기
5. 저장 위치 선택 (예: 바탕화면 또는 문서 폴더)
6. `Clone` 클릭

완료되면 폴더가 열립니다:

```
📁 vibe-coding-workshop/
├── README.md
├── 00_내_업무_구조화/
│   ├── 업무지도_작성.md
│   ├── 워크플로우_템플릿.md
│   └── 총괄_업무맵_템플릿.md
├── 01_영업미팅준비/
│   └── 실습_템플릿.md
├── 02_견적제안서/
│   └── 실습_템플릿.md
├── 03_보고서/
│   └── 실습_템플릿.md
├── 04_고객응대/
│   └── 실습_템플릿.md
└── demo/
    └── sort_downloads.py
```

---

## STEP 4 — AI 도구 계정 만들기 (5분)

### 옵션 A: Antigravity (권장, 무료, 설치 불필요)

1. [https://antigravity.ai](https://antigravity.ai) 접속
2. 이메일로 무료 가입
3. 로그인 후 새 프로젝트 만들기
4. 접속 확인 완료

### 옵션 B: Claude.ai (Antigravity가 안 될 경우)

1. [https://claude.ai](https://claude.ai) 접속
2. 이메일로 무료 가입
3. 첫 화면에서 채팅 입력창 확인

> 둘 다 무료, 설치 불필요, 브라우저만 있으면 됩니다.

---

## STEP 5 — Python 설치 (선택 — 아이스브레이킹 직접 실행용)

파일 정리 데모를 직접 실행해보고 싶다면 설치하세요.
**필수는 아닙니다** — 강사 화면으로도 충분히 볼 수 있습니다.

1. [https://python.org/downloads](https://python.org/downloads) 접속
2. `Download Python 3.x.x` 클릭
3. 설치 시 **"Add Python to PATH"** 체크 필수
4. 설치 완료 후 확인:
   - Windows: 검색창에 `cmd` → `python --version` 입력
   - Mac: 터미널 → `python3 --version` 입력
   - `Python 3.x.x` 출력되면 성공

---

## 접속 테스트 체크리스트 (교육 전날 확인)

```
□ GitHub Desktop 실행 → 실습 폴더 보임
□ Claude.ai 또는 Antigravity 접속 → 채팅창 보임
□ Chrome 또는 Edge 최신 버전
□ (선택) python --version 실행 → 버전 출력됨
```

문제가 있으면 교육 담당 강사에게 미리 연락해주세요.

---

## 자주 묻는 질문

**Q: 회사 네트워크에서 GitHub/Claude.ai 접속이 안 됩니다.**
→ 개인 스마트폰 핫스팟으로 연결하거나, 교육 당일 강사에게 문의하세요.
회사 방화벽 이슈일 수 있습니다.

**Q: Python 설치가 복잡해 보여요.**
→ 건너뛰어도 됩니다. 교육 당일 강사가 직접 실행해서 보여드립니다.

**Q: GitHub가 처음인데 어렵지 않나요?**
→ 오늘은 클론(다운로드)만 합니다. 코드 작성 없음. 버튼 클릭만으로 가능합니다.

**Q: Antigravity/Claude.ai 유료 버전이 필요한가요?**
→ 무료 버전으로 교육 내용 전체 진행 가능합니다.

---

## 강사용 — 사전 레포 설정

### 레포 생성 체크리스트

```
□ GitHub에 'vibe-coding-workshop' 레포 생성 (public)
□ 아래 폴더 구조 및 파일 업로드
□ README.md에 레포 소개 + 클론 방법 작성
□ 레포 URL 참가자에게 교육 3일 전 발송
```

### 레포 내 파일 구조

```
vibe-coding-workshop/
├── README.md                        ← 클론 방법 + 오늘 할 것 안내
├── 00_내_업무_구조화/
│   ├── 업무지도_작성.md              ← STEP 1 워크시트
│   ├── 워크플로우_템플릿.md          ← STEP 3 작성용
│   └── 총괄_업무맵_템플릿.md        ← STEP 4 작성용
├── 01_영업미팅준비/
│   └── 실습_템플릿.md               ← PART 3 실습용 프롬프트 포함
├── 02_견적제안서/
│   └── 실습_템플릿.md
├── 03_보고서/
│   └── 실습_템플릿.md
├── 04_고객응대/
│   └── 실습_템플릿.md
└── demo/
    ├── sort_downloads.py            ← 아이스브레이킹 데모 스크립트
    └── sample_downloads/            ← 데모용 샘플 파일 폴더
        ├── report.pdf
        ├── 견적서.xlsx
        └── presentation.pptx
```

### demo/sort_downloads.py 내용

```python
"""
바이브코딩 교육 — 아이스브레이킹 데모 스크립트
AI가 생성한 코드를 그대로 사용하는 예시입니다.
"""
import os
import shutil
from pathlib import Path

def sort_folder(target_folder: str):
    folder = Path(target_folder)
    
    rules = {
        "PDF": [".pdf"],
        "PPTX": [".pptx", ".ppt"],
        "엑셀": [".xlsx", ".xls"],
        "이미지": [".jpg", ".jpeg", ".png", ".gif"],
        "문서": [".docx", ".doc", ".hwp"],
    }
    
    count = 0
    for file in folder.iterdir():
        if not file.is_file():
            continue
        moved = False
        for folder_name, extensions in rules.items():
            if file.suffix.lower() in extensions:
                dest = folder / folder_name
                dest.mkdir(exist_ok=True)
                shutil.move(str(file), str(dest / file.name))
                count += 1
                moved = True
                break
        if not moved:
            other = folder / "기타"
            other.mkdir(exist_ok=True)
            shutil.move(str(file), str(other / file.name))
            count += 1
    
    print(f"정리 완료: {count}개 파일")

if __name__ == "__main__":
    # 데모용: 현재 폴더의 sample_downloads 정리
    sort_folder("sample_downloads")
```

### README.md 내용 (레포 메인)

```markdown
# 바이브코딩 워크숍 — 실습 파일

안녕하세요. 오늘 교육에 오신 것을 환영합니다.

## 이 폴더로 무엇을 할 건가요?

오늘 교육에서 여러분은:
1. `00_내_업무_구조화/` 폴더에 본인의 업무를 정리합니다
2. `01_영업미팅준비/` ~ `04_고객응대/` 중 하나를 선택해 실습합니다
3. 교육이 끝나면 이 폴더에 여러분의 결과물이 저장됩니다

## 클론(다운로드) 방법

1. GitHub Desktop 실행
2. File → Clone repository
3. URL 탭 → 이 페이지 주소 붙여넣기
4. Clone 클릭

## 문의

교육 당일 강사에게 직접 질문해주세요.
```
